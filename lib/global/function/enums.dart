enum SizeType { width, height, radius }

enum TrainingType { offlinePT, offlineConsult }

enum TrainingMode { create, edit }

enum CalendarViewMode { scheduler, table }

//for scheduler calendar
enum AppointmentTypes { rejection, consultation, pt }

enum SessionType { online, offline }

enum CurrentStatus { onlineSession, offlinePt, rejectedConsultation, offlineConsultation, blended }

enum NonTraineeNote { admin, trainer }

enum  ScheduleWidgetType{ home, traineesList,  myMembership}
